# Copy to .env.local and fill in.
#
# Without a key the app still runs end-to-end: it falls back to DEMO MODE,
# which serves pre-computed extractions for the three bundled sample calls.
# Pasting your own transcript requires a real key.
ANTHROPIC_API_KEY=

# Model used for extraction.
ANTHROPIC_MODEL=claude-opus-5
